import { ScrollPropertyDirective } from './scroll-property.directive';

describe('ScrollPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
