import { HeroImageDirective } from './hero-image.directive';

describe('HeroImageDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroImageDirective();
    expect(directive).toBeTruthy();
  });
});
